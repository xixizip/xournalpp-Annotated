# xournalpp-Annotated
Automatically export the annotated page range in Xournal++.

#插件目录 Windows: %LOCALAPPDATA%\xournalpp\plugins\

在插件目录中新建一个文件夹（可命名为 AutoExportAnnotated 或其他任意名称），然后将 main.lua 和 plugin.ini 放入该文件夹内。

之后，在菜单栏的“插件”选项下即可看到该自定义插件。点击，所有包含标注的页面的页码便会自动复制到剪贴板。
